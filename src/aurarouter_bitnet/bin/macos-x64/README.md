# BitNet — macOS Binaries

Place the BitNet-optimised `llama-server` and any companion `.dylib` libraries in this directory.

The binary should be built from llama.cpp with BitNet / 1-bit quantisation support enabled.
