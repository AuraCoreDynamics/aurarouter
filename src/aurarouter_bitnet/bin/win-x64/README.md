# BitNet — Windows x64 Binaries

Place the BitNet-optimised `llama-server.exe` and any companion DLLs in this directory.

The binary should be built from llama.cpp with BitNet / 1-bit quantisation support enabled.
